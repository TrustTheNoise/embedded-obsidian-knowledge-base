Tags: #signals #дискретность 

# Дискретный сигнал
Термин дискретный сигнал используется для обозначения сигнала, независимая переменная (чаще всего время) которого разбита на отдельные части, так что мы знаем значения сигнала только в дискретные моменты времени. Таким образом, дискретный сигнал представляется не непрерывной линией, а набором значений. Кроме разбиения на отдельные части по времени, дискретные сигналы разбиваются еще и по значению.

Мы можем пояснить эти понятия на примере. Представим себе непрерывный синусоидальный сигнал с амплитудой, равной 1, и частотой/0, описываемый уравнением 
![[Pasted image 20240430195642.png]]
Изобразив (1-1) в виде графика, мы получаем известную синусоидальную кривую, показанную на рисунке 1.1 (а). Если наш непрерывный сигнал физически представляет собой напряжение, мы можем взять его отсчеты по одному за каждые ts секунд с помощью аналого-цифрового преобразователя и представить синусоидальный сигнал в виде последовательности дискретных значений. Изобразив эти значения как точки, мы получим дискретный сигнал, показанный на рисунке 1.1 (Ь). Мы говорим, что нечто, изображенное на рисунке 1.1 (Ь), представляет собой «дискретную» версию непрерывного сигнала, показанного на рисунке 1.1 (а).  Независимая переменная £в(1-1)ина рисунке 1.1 (а) непрерывна. Независимая индексная переменная п на рисунке 1.1 (Ь) дискретна и может принимать только целые значения. То есть индекс п используется для идентификации отдельных элементов дискретной последовательности на рисунке 1.1 (Ь). 
![[Pasted image 20240430200005.png]]
![[Pasted image 20240430195956.png]]
Значения отсчетов 1-1 (b) можно представить кратко и все сразу с помощью дискретного выражения 
![[Pasted image 20240430200143.png]]
Так как полный аргумент ![[Pasted image 20240430200221.png]] представляет собой угол на который повернулся наш график в какое то время(t) с частотой(f0), и представлен угол в радианах.


# Пример в matlab
```matlab
f0 = 800;
fs = 8000;
dt = 1/fs;
t = 0:dt:0.1;
signal_cos = cos(2*pi*f0*t);
stem(t, signal_cos); xlim([0 0.009]); grid on;
```
![[Pasted image 20240501160916.png]]
*что выдаёт stem*
Этот код создаёт сигнал в виде косинуса при помощи формулы
![[Pasted image 20240430195642.png]]
Ну то же самое тока косинус
